# LuTaiPage Claude Instructions

Before analyzing, planning, editing, reviewing, or testing this project, read
and follow:

1. `AGENTS.md`
2. `.specify/memory/constitution.md`
3. The active feature files listed in the Spec Kit managed block in `AGENTS.md`
4. The confirmed UI files under `../UI设计稿已确认/`

`AGENTS.md` is the single source of truth for AI coding behavior in this
repository. Do not duplicate or reinterpret its rules here. When instructions
conflict, use the priority order defined in `AGENTS.md`.
